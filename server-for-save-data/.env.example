# CORS origin for the Next app (no trailing slash)
CLIENT_URL=http://localhost:3000

# Port for this server (optional; default 5002)
PORT=5002

# Next.js: point save-order / orders API here.
# Prefer NEXT_PUBLIC_ORDERS_SAVE_BACKEND_URL; NEXT_PUBLIC_STRIPE_BACKEND_URL still works as fallback.
# NEXT_PUBLIC_ORDERS_SAVE_BACKEND_URL=http://localhost:5002

# Writes under ./data/orders/*.json (gitignored).
