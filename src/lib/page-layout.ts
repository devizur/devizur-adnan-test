/**
 * Standard main content width + horizontal padding.
 * Use for page sections, hero blocks, navbar, and footer so columns align site-wide.
 */
export const PAGE_CONTENT_CLASS = "mx-auto container  px-4 sm:px-6";
